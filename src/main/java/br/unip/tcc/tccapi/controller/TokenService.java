package br.unip.tcc.tccapi.controller;

import br.unip.tcc.tccapi.model.Member;
//import com.auth0.jwt.JWT;
//import com.auth0.jwt.algorithms.Algorithm;
import org.springframework.boot.autoconfigure.security.oauth2.resource.OAuth2ResourceServerProperties;

import java.time.LocalDateTime;
import java.time.ZoneOffset;


public class TokenService {
//    public String generateToken(Member member) {
//        return ""; /*JWT.create()
//                .withSubject(member.getUsername()).withClaim("id", member.getId())
//                .withExpiresAt(LocalDateTime.now().plusMinutes(60L).toInstant(ZoneOffset.of("-03:00")))
//                .sign(Algorithm.HMAC256(member.getPassword()));*/
//    }
}
